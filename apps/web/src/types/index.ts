/**
 * Re-export semua types dari @airmada/types package.
 * Import dari sini di dalam apps/web agar path lebih pendek.
 *
 * @location apps/web/src/types/index.ts
 *
 * Cara pakai:
 *   import type { Vehicle, Shipment } from '@/types'
 */
export * from '@airmada/types'

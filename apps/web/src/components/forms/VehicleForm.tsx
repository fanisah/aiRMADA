/**
 * Form VehicleForm — validasi dengan Zod + react-hook-form.
 *
 * @location apps/web/src/components/forms/VehicleForm.tsx
 * TODO: Integrasi react-hook-form + zodResolver, submit ke API endpoint terkait
 */
'use client'

export function VehicleForm() {
  return (
    <form className="max-w-lg space-y-4">
      <p className="text-sm text-gray-400">VehicleForm — coming soon</p>
    </form>
  )
}
